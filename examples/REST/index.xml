<?xml version="1.0" encoding="utf-8" ?>

<cities>
    <% for(var i = 0; i < cities.length; i++) { %>
    <city>
        <id><%= cities[i].id %></id>
        <name><%= cities[i].name %></name>
        <population><%= cities[i].population %></population>
    </city>
    <% } %>
</cities>
